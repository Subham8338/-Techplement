# Remove the incorrect remote if it exists
git remote remove origin

# Add the correct remote URL for your repository
git remote add origin https://github.com/Subham8338/Techplement.git
# Push your code to the remote repository (use the correct branch name if not master)
git push -u origin master
